# docir derived index + daemon runtime — rebuildable from docs/, do not commit.
index.db
index.db-journal
index.db-wal
index.db-shm
daemon.pid
daemon.log
# Added by docir 0.27.0: files docir writes here that this store did not ignore yet.
release-check.json
feedback/
