# Entities — the core objects with identity and a lifecycle.
#
#   * document — the central aggregate: frontmatter metadata + markdown body.
#   * tag      — a registered classifier (key + description) from tags.yaml.
#   * relation — a directed edge in the document relation graph.
