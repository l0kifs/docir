# Configuration — runtime settings and the on-disk path layout.
#
#   * settings — the Settings object: resolves DOCIR_HOME (default ~/.docir)
#                into the concrete paths for the index DB, the Unix socket,
#                the PID file, the docs root, and the schema/tags files.
