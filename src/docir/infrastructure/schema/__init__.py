# Schema adapter — load docs-schema.yaml into the domain Schema object.
#
#   * default_schema — the bundled default grammar (decision/issue/architecture),
#                      written to ~/.docir on first use if none exists.
#   * loader         — parse a docs-schema.yaml file into a domain Schema.
